// api route prefix
const prefix            = 'https://api/api/'

const apiRoutes  = {
    api: {
        login                   : prefix + 'login',
        loginFace               : prefix + 'user/loginFace',
    }
}

export default apiRoutes;